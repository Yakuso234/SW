{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum(increase(sw_gateway_rate_limit_rejected_total[$__rate_interval])) by (reason)", "legendFormat": "{{reason}}"}],
      "title": "网关限流拒绝次数",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "sum(increase(sw_outbox_delivery_failures_total[$__rate_interval]))", "legendFormat": "outbox failures"}],
      "title": "Outbox 投递失败次数",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(sw_video_transcoding_seconds_bucket[$__rate_interval])) by (le))", "legendFormat": "p95"}],
      "title": "视频转码 P95 耗时",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 4,
      "targets": [{"expr": "sum(increase(sw_video_transcoding_failures_total[$__rate_interval]))", "legendFormat": "transcoding failures"}],
      "title": "视频转码失败次数",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
      "id": 5,
      "targets": [{"expr": "sum(increase(sw_video_publish_inbox_retry_total[$__rate_interval]))", "legendFormat": "delayed retries"}],
      "title": "关注流延迟重试次数",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "sw-prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16},
      "id": 6,
      "targets": [{"expr": "sum(increase(sw_video_publish_inbox_dead_letter_total[$__rate_interval]))", "legendFormat": "dead letters"}],
      "title": "关注流最终死信次数",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["sw", "microservices", "observability"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "SW 核心链路可观测性",
  "uid": "sw-core-observability",
  "version": 2
}
